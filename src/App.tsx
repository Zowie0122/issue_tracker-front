import React from "react";
import { Button } from "react-bootstrap";

function App() {
  return (
    <div>
      Issue Tracker APP
      <Button as="a" variant="success">
        Button as link
      </Button>
    </div>
  );
}

export default App;
