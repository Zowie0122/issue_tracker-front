import React from "react";

// if the user is view his/her own profile, the changable fields can be modified when click.
// if not, only for read
function UserProfile() {
  return <div>User profile</div>;
}

export default UserProfile;
